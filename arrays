const months = ['a', 't', 'h', 'e', 'l'];

PROPERTIES

Array.prototype.length 
months.length;


METHODS


months.sort();


someArray.forEach(el => {if(some logic){console.log(el));



const newArray = someArray.filter(el => el.propiedad === 'input´);

